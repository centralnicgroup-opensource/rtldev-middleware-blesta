# Composer pulls the CentralNic Reseller PHP SDK in here. The release archive
# ships it so Blesta never has to run composer, but it is built, not committed.
apis/vendor
composer.lock

build
tmp
templates_c

# The development harness lives in its own repository and is checked out here
# only while working on the module.
rtldev-middleware-blesta-dev

# PHPUnit result cache
.phpunit.cache/

# PHPUnit
.phpunit.cache/
.phpunit.result.cache
